---
"@whattax/sdk": patch
---

Add an SDK-owned downstream consumer validation command that builds and packs the SDK runtime closure, creates an external temp workspace, and reports release-blocking packed manifest protocols before claiming clean install readiness.
