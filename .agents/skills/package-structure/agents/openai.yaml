interface:
  display_name: "Package Structure"
  short_description: "Apply this repository package contract"
  default_prompt: "Use $package-structure to create or audit a package with TaxKit's repository-owned Effect, export, and verification contract."
